{% extends 'base.html.twig' %}

{% block body %}

    {{ form_start(edit_form) }}
    {{ form_widget(edit_form) }}
    <br>
    <input type="submit" value="{{ 'Update_comment'|trans }}" class="btn btn-primary" />
    {{ form_end(edit_form) }}

    <br>
    {{ form_start(delete_form) }}
    {{ form_widget(delete_form) }}
    <input type="submit" value="{{ 'Delete_comment'|trans }}" class="btn btn-danger" />
    {{ form_end(delete_form) }}

    <br>
    <a href="{{ path("articles", {"articleId": articleId} ) }}" class="btn btn-default">{{ 'back'|trans }}</a>
{% endblock %}